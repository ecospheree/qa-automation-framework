name: QA Automation

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  automation-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install Google Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Verify Chrome
        run: |
          google-chrome --version

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant permission
        run: chmod +x gradlew

      - name: Run Web Test
        run: ./gradlew webTest --stacktrace

      - name: Show failing test details
        if: always()
        run: |
          echo "=== XML Reports ==="
          find build/test-results -name "*.xml" -exec cat {} \;

      - name: Run API Test
        run: ./gradlew webTest --stacktrace --info
